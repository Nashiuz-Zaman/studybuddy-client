// containers
import InnerContainer from "./../../containers/InnerContainer/InnerContainer";

const CreateAssignment = () => {
  return (
    <div>
      <InnerContainer>
        <section>Create Assignment</section>
      </InnerContainer>
    </div>
  );
};

export default CreateAssignment;
